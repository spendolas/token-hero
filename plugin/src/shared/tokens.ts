/**
 * Token snapshot types — re-exported from protocol for convenience.
 */

export type {
  EffectToken,
  GetTokenSnapshotPayload,
  TextStyleToken,
  TokenGroupName,
  TokenSnapshotResultPayload,
} from './protocol';
